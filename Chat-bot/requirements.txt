# 📦 Project Requirements & Installation Guide

This document explains all necessary packages and how to install and run the chatbot on **Windows** and **Unix / macOS / Linux** systems.

---

## 📁 Required Files

Before installing, ensure your project folder contains:

```
mistral-chatbot/
│
├─ main.py
├─ requirements.txt
├─ .env                 # contains API key 
└─ .gitignore
```

---

## 📦 Required Packages

Your `requirements.txt` should include:

```text
mistralai
python-dotenv
```

---

## 🔐 Environment Variable Setup

Create a `.env` file in the same directory:

```text
API_KEY=your_mistral_api_key_here
```


---

# 🖥️ Installation Instructions

## 📌 1. Install Python

Ensure Python **3.9 or later** is installed.

Check version:

```bash
python --version
```

If not installed, download from:
[https://www.python.org/downloads/](https://www.python.org/downloads/)

---

## 📌 2. Create a Virtual Environment (Recommended)

### Windows (PowerShell / CMD)

```bash
python -m venv venv
venv\Scripts\activate
```

### macOS / Linux / Unix

```bash
python3 -m venv venv
source venv/bin/activate
```

---

## 📌 3. Install Dependencies

### Windows

```bash
pip install -r requirements.txt
```

### macOS / Linux / Unix

```bash
pip3 install -r requirements.txt
```

---

# ▶️ Running the Chatbot

Once installed:

### Windows

```bash
python main.py
```

### macOS / Linux / Unix

```bash
python3 main.py
```

Example:

```
You: Hello
Assistant: Hi there! How can I assist you today?
```

Exit gracefully:

```
You: quit
```

---

# 🛑 Common Issues

### ❗ `ModuleNotFoundError: No module named 'dotenv'`

Install missing package:

```bash
pip install python-dotenv
```

### ❗ `mistralai not found`

```bash
pip install mistralai
```

### ❗ API key not loading

* Check `.env` formatting:
  `API_KEY=your_key_here`
* Ensure `.env` is in the same folder as `main.py`
* Make sure `.gitignore` includes `.env`

---



